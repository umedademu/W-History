チャットのやり取りは自然言語のみの日本語でお願いします（コード内の横文字や、独自用語や専門用語を極力使わないでください）
チャットのやり取りは先に結論を答えて、その後で必要な説明をしてください
全ての実装が完了して最終報告を送る直前に、PowerShellで [console]::Beep(1200,250) を1回実行して音を鳴らしてください
毎回の実装後は、実装内容に相応しいコメントを考えて「git add . && git commit -m "◯◯" && git push」の形で、最後の1行として提案してください
毎回の実装後は、実装内容に伴ってdocsフォルダ内のドキュメントの内容も更新してください（必要な場合）
Webアプリを更新した場合は、必ず更新情報ページにも変更内容を追記してください
何らかの実装を行うたびに、このアプリのバージョン表記を0.001ずつ加算してください
毎回の実装後は、提案だけで終わらせず、自分自身でコミットとpushまで実行してください
ブラウザで動作確認する場合は、試験開始前に自動読み上げをOFFにし、音声を再生しないでください
sourcesフォルダ内のmdファイル群は原文資料として参照専用です。内容は絶対に編集しないでください（加筆・修正・削除・整形・並べ替えを含みます）。誤りや不足があるように見える場合も原文は変更せず、必要な補足や対照情報はアプリ側またはdocsフォルダ側に記録してください
画像の高精度な読み取り・書き起こし、または画像の生成・編集が作業の本質である場合に限り、Antigravity CLI経由でGemini 3.8 Flashへ依頼してください（画面確認や不具合報告用の添付画像は対象外です）

## 並列作業時の例外

複数のチャット・担当が章ごとに並列作業する場合は、次のルールを通常の完了手順より優先してください。
- 各担当は専用の作業フォルダ（Gitのワークツリー）と担当ブランチを使い、担当範囲の修正・検査・コミット・pushまで行ってください。mainへの直接のコミット・pushや、他担当の変更の統合は行わないでください。
- 各担当が編集するファイルは作業開始時に割り当て、担当範囲外の変更が必要な場合は統合担当へ報告してください。章専用の対応表・検査・画像は担当範囲に含めて構いません。
- バージョン表記、教材一覧の総ページ数、共通の更新情報・仕様書、共通処理、検査の一括実行設定など、複数章で共有するファイルは統合担当がまとめて更新してください。各担当によるバージョン加算・共通更新情報の追記は行わないでください。
- 統合担当は各章の変更と検査結果を確認してmainへ統合し、統合単位でバージョンを0.001加算し、必要なドキュメントと更新情報ページを更新して、全体の検査後にコミット・pushしてください。
- 原文資料の編集禁止、本文と地図の対応、画面確認時の音声停止など、上記以外の通常ルールは各担当にも適用します。
